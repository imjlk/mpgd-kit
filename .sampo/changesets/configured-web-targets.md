---
npm/@mpgd/cli: patch (Changed)
---

Allow game-owned `kind: "web"` target names in build, smoke, and matrix CLI workflows while preserving the existing browser and web aliases. Support additive game-owned target-config policies and static web artifact overlays for custom browser deployments.
