---
npm/@mpgd/target-config: minor (Changed)
---

Hard-rename `@mpgd/policy-matrix` to `@mpgd/target-config` and model platform feature availability with `targets.json`, target runtime snapshots, and `localization` as the public feature key.
