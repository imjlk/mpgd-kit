---
npm/@mpgd/target-config: minor (Added)
npm/@mpgd/cli: patch (Changed)
---

Add a safe-area viewport snapshot for Phaser HUD layout and make generated game
documents opt into `viewport-fit=cover` with reusable safe-area CSS variables.
Register the single-reservation contract in generated agent manifests, briefs,
and acceptance guidance so downstream games do not apply safe-area insets twice.
