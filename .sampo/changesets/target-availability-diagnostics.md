---
npm/@mpgd/target-config: patch (Changed)
---

Expose target config runtime feature snapshots so apps and smoke tests can verify which IAP, ad, and leaderboard features are available, target-disabled, or unsupported.
