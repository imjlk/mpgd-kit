---
npm/@mpgd/adapter-capacitor: minor
---

Add a scoped Capacitor native JSON HTTP transport for HTTPS GET and POST
requests, including explicit origin selection, redirect blocking, bounded
timeouts and JS-side size checks, and public Game Services runtime composition.
