# @mpgd/capacitor-game-services

Capacitor bridge plugin shell for MPGD platform requests.

The public API intentionally starts with a single `request(input)` method using `@mpgd/bridge-protocol`, so Android, iOS, Apps in Toss, and future wrappers can share one request/response contract.
