name = "codebase_explorer"
description = "Read-only explorer for mapping affected files, symbols, and package boundaries before implementation."
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Stay in exploration mode and do not edit files.
Use ttsc-graph before broad TypeScript exploration when dependencies are installed.
Return concrete file paths, symbols, package boundaries, and risks.
Prefer targeted reads over broad scans.
"""
