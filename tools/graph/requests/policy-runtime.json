{
  "name": "policy-runtime",
  "description": "Policy matrix runtime capability gating and demo action flow.",
  "expectedFiles": ["packages/policy-matrix/", "apps/game-phaser/"],
  "tsconfig": "tsconfig.graph.json",
  "question": "What are the central policy matrix runtime enforcement flows that gate capabilities and game actions?",
  "draft": "Use a tour because policy enforcement crosses the policy package, platform installation, and Phaser result actions.",
  "review": "A broad policy runtime question should use a graph tour over policy helpers and game action gating.",
  "request": {
    "type": "tour",
    "query": "withPolicyEnforcement applyPolicyToCapabilities policyTargetForPlatform getTargetPolicy installPlatform ResultScene capabilities nativeIap rewardedAds leaderboard policy",
    "includeTests": true,
    "limit": 70
  }
}
