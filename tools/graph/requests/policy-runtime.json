{
  "name": "policy-runtime",
  "description": "Policy matrix runtime capability gating, diagnostics, and demo action flow.",
  "expectedFiles": ["packages/policy-matrix/", "apps/game-phaser/"],
  "tsconfig": "tsconfig.graph.json",
  "question": "What are the central policy matrix runtime enforcement and diagnostics flows that gate capabilities and game actions?",
  "draft": "Use a tour because policy enforcement and diagnostics cross the policy package, platform installation, and Phaser result actions.",
  "review": "A broad policy runtime question should use a graph tour over policy helpers, diagnostics, and game action gating.",
  "request": {
    "type": "tour",
    "query": "withPolicyEnforcement createPolicyRuntimeSnapshot getPolicyRuntime applyPolicyToCapabilities policyTargetForPlatform getTargetPolicy installPlatform DemoState LobbyScene ResultScene capabilities nativeIap rewardedAds leaderboard policy",
    "includeTests": true,
    "limit": 70
  }
}
