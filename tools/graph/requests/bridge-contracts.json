{
  "name": "bridge-contracts",
  "description": "Bridge protocol validators, fixtures, and Apps in Toss host parsing flow.",
  "expectedFiles": [
    "packages/bridge/",
    "adapters/ait/"
  ],
  "tsconfig": "tsconfig.graph.json",
  "question": "What are the central bridge protocol validator, fixture, and Apps in Toss host parsing flows?",
  "draft": {
    "reason": "Use a tour because the bridge contract crosses protocol validators, fixtures, and Apps in Toss host parsing.",
    "type": "tour"
  },
  "review": "A bridge contract question should use a focused graph tour over validators, fixtures, and Apps in Toss host parsing.",
  "request": {
    "type": "tour",
    "includeTests": true,
    "limit": 60,
    "reinterpretations": [
      "assertBridgeRequest",
      "assertBridgeResponse",
      "BridgeRequest",
      "BridgeResponse",
      "parseBridgeRequest"
    ]
  }
}
